{
  "name": "workshop-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "cd frontend && npm run deploy",
    "db:migrate": "turso db shell workshop-platform < supabase/migrations/005_sqlite_unified.sql"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}